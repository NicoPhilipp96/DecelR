% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ada.R
\name{ada_analyze}
\alias{ada_analyze}
\title{Acceleration-to-Deceleration Assessment (ADA)}
\usage{
ada_analyze(
  data,
  cutoff_hz = 3,
  filter_order = 2,
  start_method = c("acc_threshold", "peak_speed"),
  acc_threshold = -1.5,
  stop_speed = 0.2,
  trim_speed = 0.5
)
}
\arguments{
\item{data}{A data.frame with columns: time (s), speed (m/s); optional force (N), optional position (m)}

\item{cutoff_hz}{Numeric. Butterworth low-pass cutoff frequency in Hz. Default = 3.}

\item{filter_order}{Integer. Butterworth filter order. Default = 2.}

\item{start_method}{Either "acc_threshold" (start when derived acceleration ≤ acc_threshold) or "peak_speed".}

\item{acc_threshold}{Numeric. Start-of-deceleration threshold (m/s²). Default = -1.5.}

\item{stop_speed}{Numeric. Speed threshold for end of deceleration (m/s). Default = 0.2.}

\item{trim_speed}{Numeric. Speed threshold to trim standing phase (m/s). Default = 0.5.}
}
\value{
A list with:
\itemize{
\item data: tibble with filtered columns (\code{speed_f}, derived \code{acc_f}, optional \code{force_f}, \code{position_f})
\item indices: list with \code{i_peak}, \code{i_start}, \code{i_stop}
\item metrics: tibble of ADA metrics
\item plot_time: ggplot object of speed–time with thresholds shown
\item plot_pos: ggplot object of speed–position with thresholds shown (if position exists or is derived)
}
}
\description{
Analyze time–speed (and optional position/force) data for acceleration/deceleration metrics.
Acceleration is derived from filtered speed-time (dv/dt), not from a provided acceleration column.
}
